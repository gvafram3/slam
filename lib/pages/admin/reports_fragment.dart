import 'package:flutter/material.dart';

class ReportsFragment extends StatelessWidget {
  const ReportsFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Reports Fragment'),
    );
  }
}
