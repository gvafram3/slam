import 'package:flutter/material.dart';

class TrackStudentsFragment extends StatelessWidget {
  const TrackStudentsFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Track Students Fragment'),
    );
  }
}
